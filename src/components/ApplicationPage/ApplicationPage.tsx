// Ant Design Imports

import 'antd/dist/antd.css';

// Components

// Styled Components

// Interfaces

export const ApplicationPage = (): JSX.Element => {
    const placeHolder = 'page';
    return <div>{`Welcome to the application ${placeHolder}.`}</div>;
};
